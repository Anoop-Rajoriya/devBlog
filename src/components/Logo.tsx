import { Button } from "./ui/button";

const Logo = () => {
  return (
    <Button className="p-1 px-2 rounded-none h-max font-bold capitalize">
      DevBlog
    </Button>
  );
};

export default Logo;
